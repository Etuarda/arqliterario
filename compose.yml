services:
  web:
    build: .
    container_name: arqliterario_web
    ports:
      - "3000:3000"
    volumes:
      - .:/app # Sincroniza tudo, incluindo db e storage
    environment:
      RAILS_ENV: development
    command: bin/rails server -b 0.0.0.0

