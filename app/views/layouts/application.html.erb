<!DOCTYPE html>
<html>
  <head>
    <title>Arquivo Literário</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="container">
      <div class="brand-row">
        <h1 class="brand">Arquivo Literário</h1>
        <p class="tagline">Um porto seguro para as nossas histórias lidas</p>

        <nav class="auth-nav">
          <% if user_signed_in? %>
            <span class="user-greeting">Olá, <%= current_user.name.presence || current_user.email %></span>
            <%= link_to "Sair", destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-link" %>
          <% else %>
            <%= link_to "Entrar", new_user_session_path, class: "nav-link" %>
            <%= link_to "Criar conta", new_user_registration_path, class: "nav-link" %>
          <% end %>
        </nav>
      </div>
    </header>

    <main>
      <% if notice.present? %>
        <div class="flash flash-notice"><%= notice %></div>
      <% end %>

      <% if alert.present? %>
        <div class="flash flash-alert"><%= alert %></div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
